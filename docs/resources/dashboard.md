---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "uptrace_dashboard Resource - uptrace"
subcategory: ""
description: |-
  Manages an Uptrace dashboard using YAML definition.
---

# uptrace_dashboard (Resource)

Manages an Uptrace dashboard using YAML definition.

## Example Usage

```terraform
resource "uptrace_dashboard" "example" {
  yaml = <<-YAML
    schema: v2
    name: Service Overview
    grid_rows:
      - title: Traffic
        items:
          - title: Request Rate
            metrics:
              - http_requests_total as $requests
            query:
              - per_min(sum($requests))
          - title: Error Rate
            metrics:
              - http_errors_total as $errors
            query:
              - per_min(sum($errors))
      - title: Resources
        items:
          - title: CPU Usage
            metrics:
              - system.cpu.utilization as $cpu
            query:
              - avg($cpu)
          - title: Memory Usage
            metrics:
              - system.memory.usage as $mem
            query:
              - avg($mem)
  YAML
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `yaml` (String) Dashboard YAML definition. Supports all dashboard features including grid layout, charts, tables, heatmaps, and gauges.

### Read-Only

- `created_at` (String) Dashboard creation timestamp.
- `id` (String) Dashboard identifier.
- `name` (String) Dashboard name (extracted from YAML or API response).
- `pinned` (Boolean) Whether the dashboard is pinned.
- `updated_at` (String) Dashboard last update timestamp.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Dashboard can be imported by specifying the dashboard ID
terraform import uptrace_dashboard.example 123
```
